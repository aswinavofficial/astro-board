import { useMemo } from 'react'; import { computeChart } from '../engine'; import { useProfiles } from '../state/profiles'; import { useSettings } from '../state/settings'; export function useChart(){const {profiles,active}=useProfiles(); const settings=useSettings(); const profile=profiles[active]; return useMemo(()=>profile?computeChart(profile,settings):null,[profile,settings.houseSystem,settings.zodiac]);}
