import { useChart } from '../hooks/useChart'; import { PageHeader } from '../components/layout/PageHeader'; import { GlassCard } from '../components/ui/GlassCard'; export default function Planets(){const c=useChart()!; return <><PageHeader title="Planets & Points"/><div className="grid gap-3 md:grid-cols-2">{c.planets.map(p=><GlassCard key={p.id}><b>{p.id}</b> in {p.sign} {p.house&&`· House ${p.house}`}<br/><span className="text-white/60">{p.lon.toFixed(2)}° speed {p.speed.toFixed(3)} {p.retrograde?'Rx':''}</span></GlassCard>)}</div></>}
