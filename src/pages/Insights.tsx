import { useChart } from '../hooks/useChart'; import { PageHeader } from '../components/layout/PageHeader'; import { GlassCard } from '../components/ui/GlassCard'; import { disclaimer } from '../content/keywords'; export default function Insights(){const c=useChart()!; return <><PageHeader title="Printable Insights"/><GlassCard><p>{disclaimer}</p><h3 className="mt-4 font-serif text-3xl text-gold">Synthesis</h3><p>Your chart shape reads as {c.analytics.shape}. Dominant signatures include {c.analytics.dominants.join(', ')}. The current report blends Western placements, Vedic nakshatra, dasha timing, yogas, Chinese zodiac, numerology and biorhythm extras for reflection.</p><pre className="mt-4 whitespace-pre-wrap text-sm">{JSON.stringify(c.extras,null,2)}</pre></GlassCard></>}
