import { PageHeader } from '../components/layout/PageHeader'; import { useSettings } from '../state/settings'; import { useProfiles } from '../state/profiles'; export default function Settings(){const s=useSettings(); const clear=()=>useProfiles.setState({profiles:[],active:0}); return <><PageHeader title="Settings"/><div className="glass grid gap-4 rounded-3xl p-5"><select className="field" value={s.zodiac} onChange={e=>s.set({zodiac:e.target.value as any})}><option value="tropical">Tropical</option><option value="sidereal">Sidereal Lahiri</option></select><select className="field" value={s.houseSystem} onChange={e=>s.set({houseSystem:e.target.value as any})}><option value="placidus">Placidus</option><option value="whole-sign">Whole Sign</option><option value="equal">Equal</option><option value="porphyry">Porphyry</option></select><button className="btn" onClick={clear}>Reset profiles</button></div></>}
