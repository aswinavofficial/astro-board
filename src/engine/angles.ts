import { norm } from './util'; export function angles(ramc:number,lat:number,eps:number){const R=ramc*Math.PI/180, e=eps*Math.PI/180, p=lat*Math.PI/180; const mc=norm(Math.atan2(Math.sin(R),Math.cos(R)*Math.cos(e))*180/Math.PI); let asc=norm(Math.atan2(Math.cos(R),-(Math.sin(R)*Math.cos(e)+Math.tan(p)*Math.sin(e)))*180/Math.PI); if(norm(asc-mc)>180) asc=norm(asc+180); const vertex=norm(Math.atan2(Math.cos(R+Math.PI),-(Math.sin(R+Math.PI)*Math.cos(e)+Math.tan(Math.PI/2-p)*Math.sin(e)))*180/Math.PI); return {asc,mc,vertex,ramc};}
