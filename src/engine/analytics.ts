import { elements, modalities, signIndex } from './util'; export function analytics(planets:any[],aspects:any[]){const balance:any={Fire:0,Earth:0,Air:0,Water:0,Cardinal:0,Fixed:0,Mutable:0}; for(const p of planets.slice(0,10)){const w=['Sun','Moon'].includes(p.id)?2:1; balance[elements[signIndex(p.lon)%4]]+=w; balance[modalities[signIndex(p.lon)%3]]+=w;} return {balance,dominants:planets.slice(0,3).map((p:any)=>p.id),patterns:aspects.slice(0,4).map((a:any)=>a.type),shape:'Splay'};}
